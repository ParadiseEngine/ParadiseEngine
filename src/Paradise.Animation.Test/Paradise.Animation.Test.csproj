<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AssemblyName>Paradise.Animation.Test</AssemblyName>
    <RootNamespace>Paradise.Animation.Test</RootNamespace>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="TUnit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Paradise.Animation\Paradise.Animation.csproj" />
  </ItemGroup>

  <!-- Archives written by ozz-animation 0.17's own SkeletonBuilder/AnimationBuilder from the
       procedural rig OzzParityTests regenerates: the format contract, as bytes. -->
  <ItemGroup>
    <EmbeddedResource Include="Fixtures\*.ozz" />
  </ItemGroup>
</Project>
